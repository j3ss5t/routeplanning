import {xObject} from "../../types";

export interface RouteObject {
  id: string;
  name: string;
  uri: string;
}

export interface RoutePolicyObject {
  id: string;
  name: string;
  uri: string;
}
